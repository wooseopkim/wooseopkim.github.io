---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section>
  <h3>{title}</h3>
  <slot />
</section>

<style>
  section:not(:first-child) > :first-child {
    margin-block-start: 4rem;
  }
</style>
